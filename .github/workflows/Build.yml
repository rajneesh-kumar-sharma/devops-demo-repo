name: Build,Analyze,scan
on:
    push: 
        branches:
            - master
    pull_request:
        branches:
            - master
jobs:
  build-analyze-scan:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print Hello Message
        run: echo "Hello, DevOps Demo Repo!"
        env:
          GREETING: Hello, DevOps Demo Repo!
          run: echo $GREETING

