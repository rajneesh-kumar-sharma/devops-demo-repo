name: Build,Analyze,scan
on:
    push: 
        branches:
            - master
    pull_request:
        branches:
            - master
jobs:
  build-analyze-scan:
    name: Build
    runs-on: ubuntu-latest
    steps:
        - name: Checkout repository
        - uses: actions/checkout@v4
          with:
            fetch-depth: 0
          run: echo My name is Rajneesh


